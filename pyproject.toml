[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "pconn-docs"
authors = [{ name = "Rami Mousleh", email = "rami.mousleh@gallagher-me.com" }]
dynamic = ["version", "description"]

[tool.pylint.master]
ignore = "conf.py"
